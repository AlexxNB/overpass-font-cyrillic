kind: pipeline
name: fonts-deploy

steps:
  - name: build fonts  
    image: node:10.15-slim
    commands:
      - apt install python
      - npm install
      - npm run build
  - name: publish releases
    image: plugins/github-release
    settings:
      api_key:
        from_secret: github_api_key
      files: __fonts/*